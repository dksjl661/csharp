# Todo web

Next.js client for the C# Todo API.

```bash
cp .env.example .env.local
npm run dev
```

Open <http://localhost:3000>. The API must be running at the URL configured by `NEXT_PUBLIC_API_URL`.
